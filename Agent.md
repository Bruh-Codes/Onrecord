# Agent Rules

1. Do not write more than 500 lines in a single file. Structure code into components — no dumping everything into one file.
2. Write code that is easy to understand and debug: clear names, small functions, obvious control flow.
